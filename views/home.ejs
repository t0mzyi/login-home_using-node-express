<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Home | MyApp</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap');

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #a1c4fd, #c2e9fb);
      background-size: 200% 200%;
      animation: gradientMove 8s ease infinite;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      overflow: hidden;
      margin: 0;
      color: #222;
    }

    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .circle {
      position: absolute;
      border-radius: 50%;
      opacity: 0.35;
      background: rgba(255, 255, 255, 0.5);
      animation: float 10s ease-in-out infinite;
    }

    .circle:nth-child(1) { width: 160px; height: 160px; top: 10%; left: 15%; animation-delay: 0s; }
    .circle:nth-child(2) { width: 100px; height: 100px; top: 70%; left: 75%; animation-delay: 2s; }
    .circle:nth-child(3) { width: 200px; height: 200px; top: 50%; left: -5%; animation-delay: 4s; }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-20px); }
    }

    .home-card {
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(20px);
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(31, 38, 135, 0.2);
      padding: 3rem 2.5rem;
      text-align: center;
      max-width: 400px;
      width: 90%;
      animation: fadeSlide 1.2s ease forwards;
      opacity: 0;
      position: relative;
      z-index: 2;
    }

    @keyframes fadeSlide {
      from { opacity: 0; transform: translateY(40px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .home-card h2 {
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .home-card p {
      font-size: 1rem;
      color: #555;
      margin-bottom: 2rem;
    }

    .btn-danger {
      background: linear-gradient(135deg, #ff5858, #f857a6);
      border: none;
      border-radius: 12px;
      padding: 0.8rem 1.5rem;
      font-weight: 500;
      letter-spacing: 0.3px;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .btn-danger::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: rgba(255,255,255,0.25);
      transition: left 0.5s;
    }

    .btn-danger:hover::before {
      left: 100%;
    }

    .btn-danger:hover {
      transform: translateY(-2px);
      opacity: 0.95;
    }

    footer {
      margin-top: 1.5rem;
      color: #666;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>

  <div class="circle"></div>
  <div class="circle"></div>
  <div class="circle"></div>

  <div class="home-card">
    <h2>Welcome, <%= username %> 🎉</h2>
    <p>You have successfully logged in!</p>

    <form action="/logout" method="POST">
      <button type="submit" class="btn btn-danger">Sign Out</button>
    </form>

  </div>

</body>
</html>
